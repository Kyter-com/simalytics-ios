# Copy this outside the repo and replace placeholders with private local values.
# Do not commit a filled-in env file or real 1Password item identifiers.
ASC_OP_ACCOUNT=<optional-account-domain>
ASC_OP_VAULT=<optional-vault-name>
ASC_OP_ITEM=<1password-item-id-or-name>
ASC_BYPASS_KEYCHAIN=1
